import React from "react";
import ReactDOM from "react-dom/client";

const AppLayout = <div>Hello World</div>;
const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(AppLayout);
